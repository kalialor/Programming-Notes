App.js


import "./App.css"



function App(){
    const names = ["Pedro", "Jake", "Wilmer", "Charlie"];

    return(
    <div>
        {names.map((name, key) => {
        return <h1 key={key}> {name} </h1>
        })}
        
    </div>
)
}

export default App;








        
        


the map method takes in 2 parameters the value and the key
we named value in this case: name
key={key} removes a warning. it's not important to know at the moment